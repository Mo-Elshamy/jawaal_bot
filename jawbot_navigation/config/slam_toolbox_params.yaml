slam_toolbox:
  ros__parameters:

    # Plugin & Solver Configuration
    solver_plugin: solver_plugins::CeresSolver
    ceres_linear_solver: SPARSE_NORMAL_CHOLESKY
    ceres_preconditioner: SCHUR_JACOBI
    ceres_trust_strategy: LEVENBERG_MARQUARDT
    ceres_dogleg_type: TRADITIONAL_DOGLEG
    ceres_loss_function: None
    ceres_solver_threads: 4

    # ROS Frames & Topics (Jawbot TF Standard)
    odom_frame: odom
    map_frame: map
    base_frame: base_footprint
    scan_topic: /scan
    use_map_saver: true
    mode: mapping

    # QoS Setting (Matches your RPLiDAR publisher: RELIABLE)
    use_sensor_data_qos: false
    scan_subscriber_queue_size: 20

    # Timing & TF Buffer (Generous timeout to prevent scan dropping)
    transform_timeout: 1.0
    tf_buffer_duration: 30.0
    transform_publish_period: 0.02
    map_update_interval: 2.0
    resolution: 0.05
    min_laser_range: 0.20          # Ignore robot chassis reflections
    max_laser_range: 12.0          # Max valid range for RPLiDAR

    # Thresholds to add nodes to pose graph
    minimum_travel_distance: 0.20
    minimum_travel_heading: 0.174
    scan_buffer_size: 20
    scan_buffer_maximum_scan_distance: 10.0

    # Correlation Search Space (Safe bounds to prevent pointer crashes)
    correlation_search_space_dimension: 0.5
    correlation_search_space_resolution: 0.01
    correlation_search_space_smear_deviation: 0.1

    # Loop Closure Settings
    do_loop_closing: true
    loop_search_space_dimension: 8.0
    loop_search_space_resolution: 0.05
    loop_search_space_smear_deviation: 0.03
    loop_search_maximum_distance: 4.0
    link_scan_maximum_distance: 2.0

    # Match Penalties
    distance_variance_penalty: 0.5
    angle_variance_penalty: 1.0
    fine_search_angle_offset: 0.00349
    coarse_search_angle_offset: 0.349
    coarse_angle_resolution: 0.0349
    minimum_angle_penalty: 0.9
    minimum_distance_penalty: 0.5
    use_response_expansion: true